
import React from 'react';

interface StakeholdersSubmenuProps {
  isExpanded: boolean;
  onToggle: () => void;
  role: string;
}

export const StakeholdersSubmenu: React.FC<StakeholdersSubmenuProps> = ({
  isExpanded,
  onToggle,
  role
}) => {
  // Submenu removed - duplicates eliminated
  return null;
};
